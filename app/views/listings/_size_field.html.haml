.form-group
  = label_tag :sizes
  %br/
  - if sizes.present?
    = select :listing, :size, sizes.map{ |v| [v.name] }, { include_blank: "Select Size" }, class: "form-control input-lg"
  - else
    = select :listing, :size, [], { include_blank: "There are no sizes present for #{@product_type.name}" }, class: "form-control input-lg"
